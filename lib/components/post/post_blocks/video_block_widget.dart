import 'package:flutter/material.dart';

class VideoBlockWidget extends StatelessWidget {
  const VideoBlockWidget({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
